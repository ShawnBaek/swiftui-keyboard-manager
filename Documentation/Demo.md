# Demo evidence

Recording is pending. No runtime verification is claimed yet.

Scenario: portrait iPhone simulator; focus lower fields from different scroll
positions, type into TextEditor, then demonstrate on-drag dismissal.
The demo uses synthetic text only. The final recording will identify the
device, OS and build source.
